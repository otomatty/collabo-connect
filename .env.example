# API base URL (no trailing slash). Auth is cookie-based via Better Auth.
# ローカル: 未設定または http://localhost:3000（ローカル api 利用時）
# 本番・プロトタイプ: Railway の API URL（ローカルから本番 API に繋ぐ場合は RAILWAY.md の BETTER_AUTH_TRUSTED_ORIGINS に localhost を入れる）
VITE_API_URL=https://your-api-service.up.railway.app
